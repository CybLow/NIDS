#include "../../include/packet/PacketProcessor.h"
#include <iostream>

void PacketProcessor::ProcessPacket(PacketInfo& packet) {
    // Implementation of packet processing logic
    std::cout << "Processing packet..." << std::endl;
    // Here you can add more logic to process and categorize the packets
}
